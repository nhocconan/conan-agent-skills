# loadout.txt — exactly which skills are ACTIVE in ~/.claude/skills
#
# One name per line. Resolved in order against:
#   1. ~/.conan-agent-skills      (yours)
#   2. ~/.shared-ai-skills        (gstack + other installed suites)
#   3. ~/.agents/skills           (other suites)
#
# Enforce with:  python3 ref-skills/refsync.py loadout --apply
#
# Anything present in ~/.claude/skills but NOT listed here gets unlinked. That is the
# point: upstream installers (gstack's ./setup) write their whole suite into that
# directory on every upgrade and there is no setting that stops them, so the load-out
# is re-applied after each upgrade rather than configured once.
#
# Adding a line is cheap. Adding a line you do not need is not: every active skill's
# description competes for the model's attention when choosing which skill to fire.

# ── yours (~/.conan-agent-skills) ─────────────────────────────────────────────
a11y-audit
admin-crud-standards
agent-orchestration
agent-session-backup
autonomous-loops
anti-slop-review
appstore-review-guard
backtest-integrity
bug-class-audits
coding-env-bootstrap
demo-data-craft
docs-sync
interactive-course-builder
metric-integrity
resilient-data-harvest
secure-code-audit
senior-operator
skill-miner
store-screenshots
web-perf-audit
mobile-app-playbook

# ── ref skills: thin wrappers over upstream (see each REF.md) ────────────────
ref-skills
browsing-web
shipping-changes
investigating-bugs
web-qa

# ── wrapped upstream (visual lens) ───────────────────────────────────────────
# design-qa: wraps gstack design-review. Upstream stays inactive — the wrapper reads
#   it by path. See design-qa/REF.md for why the unwrapped attempt failed.
design-qa

# ── infrastructure, not a skill ──────────────────────────────────────────────
# gstack's repo root. MUST be here: gstack's skills resolve their binary at the
# hard-coded path $HOME/.claude/skills/gstack/browse/dist/browse. Remove this line
# and `browse` (the most-used skill on this machine) reports NEEDS_SETUP and tries
# to rebuild a 60 MB binary that already exists.
# Cost, measured: it also registers gstack's router as one visible skill. That is
# a fair trade — the router is how you still reach any gstack skill explicitly.
gstack

# ── third-party kept as-is (small, well-described, non-overlapping) ──────────
context7
codex-subagent
playwright-skill
frontend-design
frontend-responsive-ui
vercel-react-best-practices
excalidraw-diagram-skill
explaining-code
intent-layer
openai-docs-skill
read-github-docs
graphify
codebase-to-course
interview-spec
spec
spec-task-breakdown
app-creator
gemini-computer-use

# ── deliberately NOT active ──────────────────────────────────────────────────
# gstack's ~74 skills: reachable by path from the wrappers above, and their
#   descriptions are too terse to route on (123 validator warnings). The binaries
#   still work; only the skill registrations are suppressed.
# qa / qa-only: consolidated into the `web-qa` wrap above — they are one job with a
#   mode switch (report vs fix), not two skills. Reachable by path from it.
# dogfood: same job as qa-only but on a second browser stack (agent-browser). Dropped
#   2026-07-25 — see web-qa/REF.md decision log. Source kept at ~/.agents/skills/dogfood.
# google-agents-cli-*: 7-skill ADK suite, unused; sources remain in ~/.agents/skills.
