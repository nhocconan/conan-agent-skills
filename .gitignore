__pycache__/
*.pyc
.DS_Store

# per-repo execution maps: employer/internal context — NEVER commit (repo is public)
senior-operator/projects/
# internal skill-planning notes — references internal systems
PROPOSALS.md
# local account map (Cowork account UUIDs / transfer log) — NEVER commit
agent-session-backup/local/
# mining watermark + generated digests: quote real conversations — NEVER commit
skill-miner/state/
# machine-specific values / anything sensitive for the env snapshot
coding-env-bootstrap/local/
